%ol
  - @talks.each do |talk|
    %li
      %span= "Presenter #{talk.presenter.name}"
      %span= "Email #{talk.presenter.email}"
      %span= "Title: #{talk.title}"
      %span= "Summary: #{talk.summary}"
      %span= "Category: #{talk.category}"
      %span= "Duration: #{talk.duration} mins"
